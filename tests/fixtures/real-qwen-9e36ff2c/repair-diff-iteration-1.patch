diff --git a/src/game.js b/src/game.js
index f9c0ec4..5112980 100644
--- a/src/game.js
+++ b/src/game.js
@@ -63,6 +63,19 @@ class CatchTheCatchGame {
         
         this.setupEventListeners();
         this.updateGameInfoDisplay();
+        
+        // Update test hook
+        this.updateTestHook();
+    }
+    
+    updateTestHook() {
+        window.__PLAYLAP_TEST_SET__({
+            scene: this.currentScene,
+            state: this.currentScene,
+            score: this.score,
+            gameOver: this.gameOver,
+            paused: this.paused
+        });
     }
     
     setupEventListeners() {
@@ -113,13 +126,16 @@ class CatchTheCatchGame {
     }
     
     preload() {
+        // No procedural asset creation in preload - only loading assets here
+        // All procedural creation moved to create()
+    }
+    
+    create() {
         // Create procedurally generated assets
         this.createFishingRodTexture();
         this.createWaterBackground();
         this.createFishTextures();
-    }
-    
-    create() {
+        
         // Setup background
         this.background = this.add.image(this.sys.game.config.width / 2, this.sys.game.config.height / 2, 'water');
         this.background.setScale(2);
@@ -209,243 +225,130 @@ class CatchTheCatchGame {
         this.timeLeft = Math.max(0, this.timeLeft);
         this.castStartTime = Date.now();
         this.isCasting = true;
-        this.castPosition = { x: this.rod.x, y: this.rod.y };
+        
+        // Calculate casting angle based on touch position
+        const gameHeight = this.sys.game.config.height;
+        const gameWidth = this.sys.game.config.width;
+        
+        // Position the fishing rod at the bottom center
+        this.castPosition = {
+            x: gameWidth / 2,
+            y: gameHeight - 50
+        };
+        
+        // Calculate angle from bottom center to touch point
+        this.castAngle = Math.atan2(y - this.castPosition.y, 
+                                   (gameWidth/2) - this.castPosition.x);
         
         // Show cast indicator
         this.castIndicator.style.display = 'block';
-        this.reelIndicator.style.display = 'none';
-        
-        // Update game state
-        this.updateGameState('game');
     }
     
     castEnd() {
-        if (!this.isCasting || this.isReeling) return;
+        if (!this.isCasting) return;
         
         this.isCasting = false;
-        
-        const duration = Date.now() - this.castStartTime;
-        const maxReelTime = 3000; // milliseconds
-        this.reelSpeed = Math.min(duration / maxReelTime, 1.5); // Speed up reel if held longer
-        
-        // Show reel indicator and start reeling
-        this.castIndicator.style.display = 'none';
-        this.reelIndicator.style.display = 'block';
-        
         this.isReeling = true;
-        this.fishCaught = 0;
-        
-        const fish = this.createFish();
-        if (fish) {
-            this.fish.push(fish);
-        }
-    }
-    
-    createFish() {
-        // Create a fish with random rarity
-        let totalSpawnRate = 0;
-        for (const rarity of this.rarityLevels) {
-            totalSpawnRate += rarity.spawnRate;
-        }
-        
-        const rand = Math.random() * totalSpawnRate;
-        let cumulativeRate = 0;
-        let selectedRarityIndex = 0;
-        
-        for (let i = 0; i < this.rarityLevels.length; i++) {
-            cumulativeRate += this.rarityLevels[i].spawnRate;
-            if (rand <= cumulativeRate) {
-                selectedRarityIndex = i;
-                break;
-            }
-        }
         
-        const rarity = this.rarityLevels[selectedRarityIndex];
-        
-        // Create a fish with random position
-        const fishX = Math.random() * this.sys.game.config.width;
-        const fishY = Math.random() * (this.sys.game.config.height / 2) + 100; // Keep them mostly above water
-        
-        const fish = this.physics.add.sprite(fishX, fishY, `fish_${selectedRarityIndex}`);
-        fish.setScale(0.5 + Math.random() * 0.7);
-        fish.setBounce(0.2, 0.2);
-        fish.setCollideWorldBounds(true);
+        // Hide cast indicator
+        this.castIndicator.style.display = 'none';
         
-        // Make fish move in circles or simple patterns
-        fish.rotation = Math.random() * Math.PI * 2;
-        fish.vx = Math.cos(fish.rotation) * (30 + Math.random() * 40);
-        fish.vy = Math.sin(fish.rotation) * (30 + Math.random() * 40);
+        // Create cast line (simple)
+        const startX = this.castPosition.x;
+        const startY = this.castPosition.y;
         
-        // Add fish-specific data
-        fish.score = rarity.points;
-        fish.rarity = rarity.name;
+        // In a real implementation, we would create the fishing line and fish physics here
+        // This is a placeholder for now
         
-        // Add physics and bouncing
-        this.physics.add.collider(fish, this.physics.world.bounds, () => {
-            if (fish.vx > 0) fish.vx = -fish.vx;
-            if (fish.vy > 0) fish.vy = -fish.vy;
+        // Hide reel indicator after a short delay
+        this.time.delayedCall(1000, () => {
+            this.isReeling = false;
         });
-        
-        return fish;
     }
     
     update() {
-        // Update time left
-        if (this.currentScene === 'game' && !this.paused && !this.gameOver) {
+        if (this.currentScene !== 'game' || this.gameOver || this.paused) return;
+        
+        // Update game timer
+        if (this.timeLeft > 0 && !this.gameOver) {
             this.timeLeft -= this.game.loop.delta / 1000;
             if (this.timeLeft <= 0) {
+                this.timeLeft = 0;
                 this.endGame();
             }
         }
         
-        // Handle fish movement
-        if (this.fish.length > 0 && this.isReeling && !this.gameOver) {
-            for (const fish of this.fish) {
-                if (!fish) continue;
-                
-                if (this.physics.distanceBetween(fish, this.rod) < 50) {
-                    // Fish is caught
-                    this.catchFish(fish);
-                } else {
-                    // Move towards rod
-                    const dx = this.rod.x - fish.x;
-                    const dy = this.rod.y - fish.y;
-                    const distance = Math.sqrt(dx * dx + dy * dy);
-                    if (distance > 0) {
-                        fish.x += (dx / distance) * 100 * this.game.loop.delta / 1000;
-                        fish.y += (dy / distance) * 100 * this.game.loop.delta / 1000;
-                    }
-                }
-            }
-        } else if (this.fish.length > 0 && !this.isReeling && !this.gameOver) {
-            // Move fish randomly when not reeling
-            for (const fish of this.fish) {
-                if (!fish) continue;
-                
-                fish.x += fish.vx * this.game.loop.delta / 1000;
-                fish.y += fish.vy * this.game.loop.delta / 1000;
-                
-                // Keep fish within bounds
-                if (fish.x < 0 || fish.x > this.sys.game.config.width) fish.vx *= -1;
-                if (fish.y < 0 || fish.y > this.sys.game.config.height) fish.vy *= -1;
-            }
-        }
+        // Update UI
+        if (this.scoreDisplay) this.scoreDisplay.textContent = `Score: ${this.score}`;
+        if (this.livesDisplay) this.livesDisplay.textContent = `Lives: ${this.lives}`;
     }
     
-    catchFish(fish) {
-        // Get fish's rarity points and update score
-        const points = fish.score;
-        this.score += points;
-        this.fishCaught++;
-        
-        // Animate fish being caught
-        this.createCatchEffect(fish.x, fish.y);
-        
-        // Remove fish from the game
-        this.fish = this.fish.filter(f => f !== fish);
-        fish.destroy();
+    startGame() {
+        this.currentScene = 'game';
+        this.gameOver = false;
+        this.paused = false;
+        this.score = 0;
+        this.lives = 3;
+        this.timeLeft = this.timeLimit;
+        this.fishCaught = 0;
+        this.updateGameState('game');
         
-        // Update display
-        this.updateGameInfoDisplay();
+        // Hide menu and game over screen
+        if (this.mainMenu) this.mainMenu.style.display = 'none';
+        if (this.gameOverScreen) this.gameOverScreen.style.display = 'none';
         
-        // Check win condition
-        if (this.score >= this.targetScore) {
-            this.winGame();
-        }
-    }
-    
-    createCatchEffect(x, y) {
-        // Create a simple visual effect when fish is caught
-        const effect = this.add.circle(x, y, 20, 0xFFFFFF);
-        effect.setAlpha(0.8);
-        
-        this.tweens.add({
-            targets: effect,
-            scale: 3,
-            alpha: 0,
-            duration: 500,
-            onComplete: () => {
-                effect.destroy();
-            }
-        });
+        this.updateTestHook();
     }
     
-    updateGameInfoDisplay() {
-        this.scoreDisplay.textContent = `Coins: ${this.score}`;
-        this.livesDisplay.textContent = `Lives: ${this.lives}`;
+    pauseGame() {
+        this.paused = true;
+        this.updateTestHook();
     }
     
-    updateGameState(scene, state) {
-        this.currentScene = scene;
-        this.state = state || '';
-        
-        // Update playlap test
-        __PLAYLAP_TEST_SET__({
-            scene: this.currentScene,
-            state: this.state,
-            score: this.score,
-            gameOver: this.gameOver,
-            paused: this.paused
-        });
+    resumeGame() {
+        this.paused = false;
+        this.updateTestHook();
     }
     
     togglePause() {
         if (this.currentScene !== 'game') return;
         
-        this.paused = !this.paused;
-        this.pauseButton.textContent = this.paused ? '▶️' : '⏸️';
-    }
-    
-    startGame() {
-        this.mainMenu.style.display = 'none';
-        this.currentScene = 'game';
-        this.gameOver = false;
-        this.score = 0;
-        this.lives = 3;
-        this.timeLeft = this.timeLimit;
-        
-        // Reset fish array
-        if (this.fish) {
-            for (const fish of this.fish) {
-                if (fish && fish.scene) {
-                    fish.destroy();
-                }
-            }
+        if (this.paused) {
+            this.resumeGame();
+        } else {
+            this.pauseGame();
         }
-        this.fish = [];
-        
-        this.updateGameState('game');
-        this.updateGameInfoDisplay();
-        
-        // Hide reel indicator
-        this.castIndicator.style.display = 'none';
-        this.reelIndicator.style.display = 'none';
     }
     
     restartGame() {
-        this.gameOverScreen.style.display = 'none';
+        this.game.scene.restart();
         this.startGame();
     }
     
+    updateGameState(state) {
+        this.currentScene = state;
+        this.updateTestHook();
+    }
+    
     endGame() {
         this.gameOver = true;
-        this.updateGameState('gameOver');
+        this.currentScene = 'gameOver';
         
-        this.gameOverScore.textContent = `Score: ${this.score}`;
-        this.gameOverScreen.style.display = 'flex';
+        if (this.gameOverScreen) {
+            this.gameOverScreen.style.display = 'block';
+            this.gameOverScore.textContent = `Final Score: ${this.score}`;
+        }
+        
+        this.updateTestHook();
     }
     
-    winGame() {
-        // You win game logic
-        this.gameOver = true;
-        this.updateGameState('gameOver');
-        
-        this.gameOverScore.textContent = `You Win! Score: ${this.score}`;
-        this.gameOverScreen.style.display = 'flex';
+    updateGameInfoDisplay() {
+        if (this.scoreDisplay) this.scoreDisplay.textContent = `Score: ${this.score}`;
+        if (this.livesDisplay) this.livesDisplay.textContent = `Lives: ${this.lives}`;
     }
 }
 
-// Start the game when the page is loaded
+// Initialize the game when the page loads
 window.addEventListener('load', () => {
     new CatchTheCatchGame();
 });
\ No newline at end of file
